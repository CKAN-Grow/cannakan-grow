# ICE-GI-001-1A — Grow Identity Execution Boundary and Connected-Target Identity Addendum

**Identifier:** ICE-GI-001-1A
**Title:** Grow Identity Execution Boundary and Connected-Target Identity Addendum
**Status:** Approved — Architecture and Execution-Governance Audit Passed; Founder Approved; Repository Adopted; Connected Execution Not Authorized
**Subordinate to:** [ICE-GI-001-1 — Grow Identity Layer Phase 1 Present-Day Conformance Recovery](./ICE-GI-001-1-grow-identity-layer-phase-1-present-day-conformance.md)
**Governing contract:** [IC-GI-001 — Grow Identity Layer Phase 1 Implementation Contract](../implementation-contracts/IC-GI-001-grow-identity-layer-phase-1.md)
**Execution authority:** None
**Target-binding verification date:** 2026-07-31

## 1. Document Identity and Governance State

ICE-GI-001-1A is an approved and repository-adopted subordinate
execution-governance addendum. The original proposal addressed two
execution-governance ambiguities that had prevented an effective
execution-authorization decision for ICE-GI-001-1:

1. the governed connected Supabase target was not explicitly and
   authoritatively identified; and
2. the exact repository paths permitted to change during a future execution
   and closure lifecycle were not established.

The initial Architecture and Execution-Governance Audit returned
`BLOCKED — ICE-GI-001-1A REQUIRES REVISION`. A bounded correction then recorded
only the six authorized audit corrections. The repeat strict read-only audit
returned `PASS — ICE-GI-001-1A ARCHITECTURE AND EXECUTION-GOVERNANCE AUDIT`
on the exact corrected substantive bytes, and the founder explicitly approved
those exact audited substantive bytes. This repository adoption records those
completed decisions without constituting an execution preflight,
execution-authorization decision, sequence-resolution design, migration,
deployment, recovery, production action, verification result, founder
acceptance, or execution result.

The completed audit authenticated that the connected migration ledger
terminates at `20260715224500`, that ten later local migrations form the
pending tail, and that neither Grow Identity migration appears in the remote
ledger. The proposed `20260731120000` recovery migration would follow all ten
pending migrations and is not presently eligible for ordinary forward
execution. No pending predecessor gains execution authority from chronology.

ICE-GI-001-1 remains architecturally approved and founder-approved but unexecuted.
The prior execution-authorization result remains blocked and ineffective.
ICE-SC-003 remains stopped at Phase 0.

## 2. Governing Relationship and Inherited Authority

IC-GI-001 remains the sole direct source of canonical Grow Identity meaning.
ICE-GI-001-1 remains the approved recovery authority, including its 48
invariant classifications, recovery obligations, phases, gates, preservation
requirements, stop conditions, verification matrix, and closure criteria.

This addendum inherits those authorities without changing them. It supplies
two execution coordinates that ICE-GI-001-1 required a later exact
authorization to provide: one target identity and one repository modification
allowlist. It neither replaces nor supersedes IC-GI-001 or ICE-GI-001-1.

If this addendum conflicts with canonical meaning in IC-GI-001 or with the
approved recovery in ICE-GI-001-1, the conflict is a stop condition. This
addendum cannot be used to reinterpret the higher authority.

## 3. Exact Blocker-Resolution Scope

Sections 6 and 7 record the approved target identity, and Section 9 records the
approved repository-path boundary. Those coordinates do not resolve migration
sequence eligibility and do not make this approved addendum effective as
execution authorization.

The completed audit authenticated the target ledger only within its exact
read-only boundary: the remote ledger ends at `20260715224500`, the ten later
local migrations remain pending, and neither Grow Identity migration appears
remotely. The established migration mechanism cannot presently reach the
proposed recovery migration without encountering that preceding pending
sequence. Execution therefore remains stopped unless a separately governed
and audited migration-sequence resolution establishes a safe and authorized
path. This addendum grants no authority to design, approve, or apply such a
resolution.

No other blocker, readiness fact, implementation choice, or production fact is
resolved here. In particular, this correction does not establish the target's
schema, RLS, policies, grants, function definitions, data categories, backup
state, recovery state, partial-application state, or readiness for migration.
Those facts belong to a later fresh strict read-only preflight under separate
authority.

## 4. Non-Supersession and Non-Expansion

This addendum does not:

- change canonical Identity meaning or any invariant classification;
- introduce Product or Presentation behavior;
- select corrective SQL or a new database architecture;
- add a field, vocabulary, visibility state, relationship meaning,
  Recognition meaning, or source-domain authority;
- authorize either Grow Identity migration or any of the other eight pending
  migrations;
- authorize implementation, migration execution, deployment, production
  mutation, or connected-environment mutation;
- authorize broad cleanup, refactoring, formatting, dependency changes, or
  generated repository content; or
- restart or authorize ICE-SC-003.

It does not authorize P1, alter P1's withheld present-day acceptance, change
the IC-GC-002C six-resource security-correction branch, or generate, compare,
replace, or approve a global security fingerprint or security-count
checkpoint.

The exact future migration filename selected below establishes only a
repository path boundary. It does not establish the SQL that the file may
contain.

## 5. Distinct Authorities

For this addendum:

- **Architecture authority** establishes canonical meaning and permitted
  architectural boundaries. It remains in the committed governing chain and
  is not created by implementation evidence.
- **Recovery authority** establishes approved recovery obligations and remains
  in ICE-GI-001-1. This addendum does not replace or expand it.
- **Implementation authority** is separately granted permission to modify
  exact implementation paths for approved recovery work. It is absent.
- **Repository adoption** is the separately authorized recording and Git
  closure of exact approved governance bytes. It is completed for this exact
  addendum and does not imply implementation or connected execution.
- **Founder approval** is an explicit founder decision on exact audited bytes.
  It is completed for this exact addendum and does not imply connected
  execution.
- **Connected-target execution authority** is separately granted permission to
  execute an exact verified operation against an exact authenticated target.
  It is absent.
- **Execution success** is authenticated technical completion of an authorized
  operation. It is not verification, founder acceptance, or deployment.
- **Post-execution verification authority** is separately granted permission
  to perform exact post-write checks against an exact target. It is absent and
  is distinct from execution authority and execution success.
- **Founder acceptance** is a later explicit founder disposition of exact
  verified evidence. It is absent and is not supplied by technical success or
  governance closure.
- **Present-day production acceptance** is explicit recognition of exact
  implementation as the accepted current production baseline. It is absent;
  historical implementation existence does not provide it.
- **Application deployment authority** is separately granted permission to
  deploy exact verified application bytes and configuration. It is absent and
  is not supplied by migration or verification success.
- **Migration-history authority** is permission to alter, mark, repair,
  baseline, or reconstruct migration history. No such authority exists under
  this addendum.
- **Repository modification authority** is separately granted permission to
  modify an exact path listed in Section 9 under its activation condition.
  This proposal grants none by itself.
- **Connected-environment inspection authority** is separately granted
  read-only access to the exact target for a stated gate. Target binding does
  not make that authority perpetual.
- **Production authority** is separately granted permission for a specified
  production action, operator, timing, recovery boundary, and evidence record.
  It is absent.
- **Post-execution governance closure** is separately granted permission to
  record a verified complete or interrupted result in the exact closure paths.
  It is absent and does not constitute founder or present-day production
  acceptance.

None of these states implies another unless an exact governing artifact or
explicit founder decision establishes that relationship.

## 6. Exact Governed Connected Target

Exactly one Supabase project satisfies the authoritative evidence:

| Identity property | Bound value |
| --- | --- |
| Environment role | Production target for the Grow application |
| Supabase project reference | `tlchyfyvkfaakqtnhndk` |
| Public project hostname | `tlchyfyvkfaakqtnhndk.supabase.co` |
| Supabase project name | `ckan-grow-app` |
| Supabase organization | `CKAN LLC` |
| Supabase organization identifier | `lepxhsayzdlhezmmtqzc` |
| Supabase region | `us-west-2` |
| Supabase project status observed on 2026-07-31 | `ACTIVE_HEALTHY` |
| Production deployment provider | Vercel |
| Vercel team | `Don - CKAN's projects` (`team_eU33JYfjpweTZjSiSm2njgQd`) |
| Vercel project | `cannakan-grow` (`prj_JoLSSHKJQAQcPqkUBrfqVtEtRcDt`) |
| Production alias inspected | `cannakan-grow.vercel.app` |
| Production deployment | `dpl_GvDKjYJ7MriZ7Vt6a8zVzsGA59vx` |
| Production deployment hostname | `cannakan-grow-6fvkjl1ms-don-ckans-projects.vercel.app` |
| Deployment state and target | `READY`; `production` |
| Deployment creation time | `2026-07-31T11:54:30.027Z` |

This is the sole approved governed connected target for ICE-GI-001-1. Preview,
branch, local, disposable, and any other Supabase projects are not the governed
production target.

## 7. Safe Authoritative Target Evidence

Target identity was established on 2026-07-31 by the following read-only
agreement:

1. The connected Vercel account identified one project named `cannakan-grow`
   in the named team. Its latest deployment was reported as `READY` with target
   `production`, and the project listed `cannakan-grow.vercel.app` among its
   domains.
2. An HTTPS GET of `/supabase-config.js` through that production alias returned
   HTTP 200 and contained exactly one public Supabase project hostname. Only
   the non-secret hostname and its project reference were extracted; no key or
   other environment-variable value was recorded.
3. The already-configured Supabase account returned exactly one project whose
   reference matched that production hostname. Its non-secret metadata is the
   project name, organization, region, and active status in Section 6.
4. The untracked local linkage file `supabase/.temp/project-ref` was compared
   without quoting its contents and agreed with the authoritative reference.
   It is corroboration only and is not sufficient target authority.
5. No committed canonical repository document supplied an independent project
   reference. Repository silence neither conflicts with nor independently
   establishes the target.

Production deployment configuration and Supabase project metadata therefore
agree on one project reference and one public hostname. No secret, credential,
private connection value, personal user data, schema state, or row data was
included in the evidence.

## 8. Target-Binding Rules

Every later preflight, migration plan, deployment plan, command, evidence
record, and closure record must identify the Section 6 reference and hostname.
The future strict read-only preflight must re-resolve the production deployment
binding and Supabase metadata rather than trusting this observation alone.

The target binding fails closed if the production alias points to another
reference or hostname, Supabase metadata no longer resolves exactly one
matching project, the deployment role is not production, the configured
account or organization differs, or target identity depends on local linkage
alone. No command may relink the repository to cure a mismatch.

This target binding does not authorize inspection beyond a separately stated
read-only gate and does not authorize any write.

## 9. Exhaustive Repository Modification Allowlist

The following table is the complete approved modification allowlist for the
ICE-GI-001-1 approval-recording, execution, verification, production, and
closure lifecycle. Each activation condition must be satisfied in addition to
all higher-authority gates and a separate instruction authorizing the
particular action.

| Exact repository-relative path | State | Purpose and ICE phase | Classification and activation condition | Permitted modification | Prohibited within the path |
| --- | --- | --- | --- | --- | --- |
| `docs/foundation/implementation-contract-executions/ICE-GI-001-1A-grow-identity-execution-boundary-and-connected-target-identity-addendum.md` | Existing after this authorship | Approval-state recording before Phase 0 | Mandatory only after a PASS audit, explicit founder approval of exact substantive bytes, and separate repository-closure instruction | Metadata-only approval/status normalization and decision-history recording | Any change to target identity, path/object boundary, obligations, gates, or substantive authority |
| `docs/foundation/implementation-contract-executions/README.md` | Existing | Addendum registration before Phase 0; exact status after Phase 8 | Mandatory only under separate repository-closure authority after approval; later status change only after verified complete or interrupted closure | Add or update only the exact ICE-GI-001-1A registry entry and the exact ICE-GI-001-1 execution-status text | Reordering or changing other entries, authority, or descriptive content |
| `supabase/migrations/20260731120000_grow_identity_phase1_present_day_conformance_recovery.sql` | Selected future path; absent on 2026-07-31 | Potential forward-only corrective migration for Phases 2–4; not presently eligible for creation or execution | Inactive unless every addendum gate and a separately authorized and audited migration-sequence resolution later establish eligibility | No present modification; future creation would require separate exact implementation authority after every applicable gate | Rewriting earlier migrations; another capability; ledger repair; sequence reconstruction; execution-selection workaround; invented truth; destructive or speculative data changes; any unlisted connected object |
| `app.js` | Existing | Retire or constrain the confirmed Profile direct-upsert bypass and consume the corrected privacy-safe interfaces; Phases 3 and 5 | Mandatory because E5 constructs and directly upserts `profile_type` through the confirmed legacy path | Minimum write-route and response-consumer compatibility edits required by invariants 12–20, 28, 31, 33–36, and 46 | Product redesign, unrelated refactoring, new Identity meaning, client-only privacy, service credentials, or changes outside Grow Identity consumers |
| `src/grow-identity-contract.js` | Existing | Client/database contract parity; Phases 2–5 | Conditional only if the Phase 1 parity comparison proves that a governing correction changes an already mirrored contract value | Change only the existing mirrored value required for exact parity with the corrected canonical database contract | New fields, vocabulary, defaults, version meaning, or speculative cleanup |
| `scripts/grow-identity-phase1-regression-check.mjs` | Existing | Focused correction and preservation coverage; Phases 1–5 | Mandatory before any Phase 6 entry | Add fixtures and assertions for all confirmed defaults/backfill, visibility, payload, search, mutation, provenance, Recognition, Vault-purpose, rerun, and interruption failures | Weakening existing assertions, accepting fabricated truth, broad unrelated fixtures, or target writes |
| `scripts/security-access-regression-check.mjs` | Existing | RLS, relation/column privilege, anonymous, authenticated, and service-role boundaries; Phases 3 and 5 | Mandatory because grants and public/canonical access are confirmed correction surfaces | Add or update exact expectations for the corrected Identity surfaces | Broad security exemptions, unrelated capability expectations, credentials, or mutation of a connected target |
| `scripts/security/function-access-audit.mjs` | Existing | Complete effective-function audit; Phase 5 | Conditional only if execution evidence proves the existing generic audit cannot express an ICE-required check for an already listed function | Minimum audit logic for exact owner, grant, search-path, authorization, and output checks | New function authority, exemptions that weaken the audit, or unrelated function-policy changes |
| `scripts/community-grow-profile-integration-regression-check.js` | Existing | Downstream Community/Profile privacy compatibility; Phase 5 | Conditional only if an existing assertion fails solely because an authorized `app.js` correction changes the privacy-safe Identity consumption route | Update the minimum affected assertion while preserving or strengthening privacy and source-domain separation | Community behavior changes, weaker privacy, or unrelated Presentation work |
| `scripts/profile-hero-catalog-regression-check.js` | Existing | Effective public Profile projection and migration-order compatibility; Phases 2 and 5 | Mandatory because the selected forward migration becomes the effective definition after E2 | Add the selected migration to ordering/effective-projection checks and assert fail-closed public output | Catalog redesign, asset changes, broader projection, or modification of E1 or E2 |
| `docs/foundation/implementation-contract-executions/ICE-GI-001-1-grow-identity-layer-phase-1-present-day-conformance.md` | Existing | Truthful complete or interrupted execution record; Phase 8 | Closure-only after all applicable verification gates finish, under separate governance-closure authority | Status and decision/execution-history recording of exact paths, commit, migration, target, checks, and active authority | Reclassification of invariants, recovery-scope changes, retrospective authorization, or alteration of approved obligations |
| `docs/production/grow-production-ledger.md` | Existing | Required production ledger entry; Phase 8 after Phase 7 | Conditional on actual separately authorized production execution, verified outcome, and separately applicable production-ledger authority | Append one exact, non-secret complete or interrupted ICE-GI-001-1 production record | Editing prior entries, recording planned work as completed, secrets, personal data, or unrelated production activity |

Every repository path not listed in this table is prohibited from modification.
An activated conditional path does not activate another conditional path.

`scripts/security/approved-security-fingerprint.json` is not modification-
authorized by this addendum. The currently approved fingerprint remains
`40b325fb4240387ed6eed8f74366f5fd61d3cf886c5f1b1f7f14ac203d024f56`,
with approved counts `48 / 1 / 0 / 105 / 142 / 5`. This addendum cannot
generate, compare, recalculate, replace, supersede, reapprove, or otherwise
alter that approved state.

## 10. Client or Consumer Compatibility Boundary

The only mandatory client path is `app.js`. It is required by the affirmative
E5 defect: the Profile consumer constructs `profile_type` and sends it through
the broad direct `public_member_profiles` upsert. The permitted correction must
route supported owner-controlled Identity values through the approved
canonical mutation boundary, stop sending protected or non-allowlisted values,
and consume only the corrected server-filtered result.

`src/grow-identity-contract.js` is conditional solely for demonstrated parity,
not for new design. No modification is authorized for `index.html`,
`service-worker.js`, `supabase-config.js`, `scripts/build-config.mjs`, or any
other consumer, build, cache, deployment, or configuration path. The current
service worker uses network-first delivery for `/app.js`; no cache-bust edit is
required by present evidence. A build that would rewrite `supabase-config.js`
in the governed checkout must not be run there.

## 11. Regression, Security, and Evidence Boundary

The mandatory regression modifications are limited to:

- `scripts/grow-identity-phase1-regression-check.mjs` for all confirmed
  Identity defects and preservation obligations;
- `scripts/security-access-regression-check.mjs` for the effective access and
  role boundary;
- `scripts/profile-hero-catalog-regression-check.js` for the effective public
  projection and migration order.

The conditional scripts in Section 9 may change only under their exact
conditions. The following existing paths may be inspected as read-only
evidence and are not modification-authorized:

- `scripts/security/run-security-verification.mjs`;
- `docs/grow-function-access-audit-2026-07-15.md`;
- `package.json`; and
- `docs/grow-identity-layer-phase1.md`.

`scripts/security/security-fingerprint.mjs` and
`scripts/security/approved-security-fingerprint.json` remain protected
read-only repository evidence. This addendum authorizes neither execution of
the fingerprint tool nor comparison, certification, or replacement of the
approved fingerprint or counts. Security-fingerprint governance remains
separate from Grow Identity recovery and requires separate authority that
expressly preserves and sequences the IC-GC-002C/P1 branch.
No connected security-resource inspection or certification is authorized.
Because `scripts/security/run-security-verification.mjs` invokes the security
fingerprint command, this addendum does not authorize executing that aggregate
runner. Any future security or fingerprint verification requires its own exact
authority and must preserve the approved checkpoint and the IC-GC-002C/P1
branch.

No new test, report, manifest, snapshot, package, lockfile, or evidence path is
authorized. Execution evidence must be recorded in the exact closure paths in
Section 9.

## 12. Execution-Phase-to-Path Mapping

| ICE phase | Modification-authorized paths after all gates pass |
| --- | --- |
| Phase 0 — repository, authority, and evidence preflight | None |
| Phase 1 — reproducible local baseline | None; listed scripts and migrations are read-only execution evidence |
| Phase 2 — privacy, public projection, and search recovery | `supabase/migrations/20260731120000_grow_identity_phase1_present_day_conformance_recovery.sql`; `scripts/grow-identity-phase1-regression-check.mjs`; `scripts/profile-hero-catalog-regression-check.js`; conditional `src/grow-identity-contract.js` |
| Phase 3 — mutation, grants, protected fields, and provenance | The selected migration; `app.js`; `scripts/grow-identity-phase1-regression-check.mjs`; `scripts/security-access-regression-check.mjs`; conditional `src/grow-identity-contract.js` |
| Phase 4 — records, backfill, idempotency, and interruption | The selected migration and `scripts/grow-identity-phase1-regression-check.mjs`; conditional `src/grow-identity-contract.js` |
| Phase 5 — full local verification and compatibility | `scripts/grow-identity-phase1-regression-check.mjs`; `scripts/security-access-regression-check.mjs`; `scripts/profile-hero-catalog-regression-check.js`; conditional `scripts/security/function-access-audit.mjs`; conditional `scripts/community-grow-profile-integration-regression-check.js`; and conditional `src/grow-identity-contract.js` |
| Phase 6 — connected-target and production-readiness gate | None; connected inspection is read-only under separate authority |
| Phase 7 — target migration and post-migration verification | Inaccessible unless a separately authorized and audited migration-sequence resolution first establishes eligibility; migration execution, post-execution verification, and application deployment remain separately authorized activities |
| Phase 8 — closure and interruption | ICE-GI-001-1, the execution index, and, only when applicable, the production ledger |

The addendum and execution-index pre-execution approval normalization occur
before Phase 0 under separate repository-closure authority, not as execution.

## 13. Exact Read-Only Repository Evidence

The following exact paths are governing or implementation evidence and remain
read-only unless separately listed and activated in Section 9:

- `docs/platform/grow-platform-architecture.md`;
- `docs/foundation/grow-foundation.md`;
- `docs/philosophy/grow-philosophy.md`;
- `docs/foundation/grow-platform-milestones.md`;
- `docs/product/profile-framework/PF-003-profiles-relationships-and-access.md`;
- `docs/product/profile-experience-framework/profile-experience-framework.md`;
- `docs/product/profile-experience-framework/visual-language.md`;
- `docs/governance/grow-architecture-governance.md`;
- `docs/foundation/implementation-contracts/IC-GI-001-grow-identity-layer-phase-1.md`;
- `docs/foundation/implementation-contract-executions/ICE-GI-001-1-grow-identity-layer-phase-1-present-day-conformance.md` until its Phase 8 gate;
- `docs/foundation/implementation-contract-executions/ICE-SC-003-current-conditions-operations-and-forward-only-legacy-declaration.md`;
- `docs/foundation/implementation-contract-executions/README.md` until its applicable closure gate;
- `supabase/migrations/20260718120000_grow_identity_layer_phase1.sql`;
- `supabase/migrations/20260719140000_profile_hero_catalog_safe_projection.sql`;
- `supabase/migrations/20260623124000_shared_seed_vault_direct_user_shares.sql`;
- `src/grow-identity-contract.js` unless its Section 9 condition activates;
- `app.js` outside its exact activated correction;
- `scripts/grow-identity-phase1-regression-check.mjs` outside its exact activated coverage;
- `scripts/security-access-regression-check.mjs` outside its exact activated coverage;
- `scripts/security/function-access-audit.mjs` unless its Section 9 condition activates;
- `scripts/security/approved-security-fingerprint.json` as a protected
  checkpoint that this addendum cannot generate, compare, recalculate,
  replace, supersede, reapprove, or otherwise alter;
- `scripts/security/run-security-verification.mjs`;
- `scripts/security/security-fingerprint.mjs`;
- `docs/grow-function-access-audit-2026-07-15.md`;
- `scripts/community-grow-profile-integration-regression-check.js` unless its Section 9 condition activates;
- `scripts/profile-hero-catalog-regression-check.js` outside its exact activated coverage;
- `package.json`;
- `docs/grow-identity-layer-phase1.md`;
- `index.html`;
- `service-worker.js`;
- `scripts/build-config.mjs`;
- `supabase-config.js`;
- `vercel.json`;
- `supabase/config.toml`;
- `supabase/.temp/project-ref` as untracked local corroboration only; and
- `docs/production/grow-production-ledger.md` until its Section 9 condition
  activates.

The completed audit authenticated that the connected ledger terminates at
`20260715224500` and that the following ten later local migrations are the
pending tail, in chronological order:

1. `20260718120000_grow_identity_layer_phase1.sql`;
2. `20260719140000_profile_hero_catalog_safe_projection.sql`;
3. `20260721180000_grow_companion_phase1.sql`;
4. `20260723100000_grow_session_entry_path.sql`;
5. `20260723120000_growing_phase_and_plant_groups.sql`;
6. `20260723150000_workspace_tasks_canonical_semantics.sql`;
7. `20260723160000_workspace_events_canonical_semantics.sql`;
8. `20260723170000_growing_workspace_notes.sql`;
9. `20260727120000_canonical_growing_commencement.sql`; and
10. `20260728120000_session_conditions_first_canonical_slice.sql`.

Neither Grow Identity migration appears in the authenticated remote ledger.
Both remain historical implementation evidence with present-day acceptance
withheld. All ten migrations are read-only dependency and ordering evidence
and are prohibited from connected execution or modification by this addendum.
The fifth migration is P1; P1 remains unauthorized and retains withheld
present-day acceptance. Chronological position grants no migration execution
authority.

## 14. Connected Schema and Migration-Ledger Boundary

The governed schema is `public`. The Supabase-managed migration ledger
`supabase_migrations.schema_migrations` is read-only during preflight and
verification. The completed audit authenticated a terminal remote version of
`20260715224500` and ten later local pending migrations. Neither
`20260718120000` nor `20260719140000` appears in the authenticated remote
ledger. The proposed `20260731120000` recovery migration would follow all ten
pending versions and is not presently eligible for ordinary forward
execution.

There is currently no permitted new ledger consequence under this addendum.
The established migration mechanism cannot reach `20260731120000` without
encountering the preceding pending sequence, and chronology supplies no
authority to apply any predecessor. Execution remains stopped unless a
separately authorized and audited migration-sequence resolution establishes a
safe path. This addendum neither designs nor authorizes that resolution.

Manual ledger insertion, deletion, repair, reconciliation, marking, or
retrospective acceptance of E1, E2, E16, or any other migration is prohibited.
The `auth` schema, `auth.users`, `auth.uid()`,
`public.current_user_is_admin()`, and `pg_timezone_names` are read-only static
dependencies. Their connected presence and compatibility have not been
inspected by this correction and must be authenticated only during a
separately authorized future connected preflight. The `pgcrypto` extension is
read-only evidence and prohibited from mutation.

## 15. Connected Table, Column, Constraint, and Index Boundary

### 15.1 Permitted forward-only correction surfaces

Subject to a later verified implementation design, the selected migration may
alter only the existing definitions, defaults, grants, policies, or truthful
data treatment of these exact Identity surfaces:

- table `public.public_member_profiles`, limited to columns `id`, `user_id`,
  `display_name`, `username`, `public_handle`, `avatar_url`,
  `cover_image_url`, `bio`, `primary_role`, `experience_level`,
  `years_growing`, `languages`, `growing_environments`,
  `favorite_germination_methods`, `grow_interests`, `grow_goals`,
  `favorite_breeders`, `favorite_sources`, `city`, `state_province`,
  `country`, `country_code`, `location_region`, `timezone`,
  `profile_visibility`, `grow_network_discoverable`,
  `connection_request_permission`, `invitation_preferences`,
  `personalization_consent`, `identity_provenance`,
  `show_profile_in_community_grow`, `allow_followers`,
  `show_grow_stats_publicly`, `notify_community_activity`, `profile_type`,
  `account_type`, `is_verified`, `reserved_grow_id`, `vault_theme`,
  `grow_id_qr_data_url`, `grow_id_qr_profile_url`,
  `grow_id_qr_updated_at`, `joined_at`, `created_at`, and `updated_at`; and
- table `public.grow_identity_field_visibility`, limited to columns `user_id`,
  `field_key`, `visibility`, `created_at`, and `updated_at`.

Data mutation is narrower than schema reference. `id`, `user_id`,
`is_verified`, `reserved_grow_id`, `grow_id_qr_data_url`,
`grow_id_qr_profile_url`, and `grow_id_qr_updated_at` are
preservation/verification surfaces and are prohibited from corrective
reassignment or rewriting. Their column privileges may only be revoked or
narrowed. Valid owner association, Recognition state, identifiers, privacy
choices, and provenance must be preserved. A row may change only after the
later preflight and Phase 1 category model prove the change is required,
truthful, evidence-preserving, privacy-non-broadening, deterministic, and
safely rerunnable.

The exact constraints within the correction surface are:

- `public_member_profiles_visibility_check`;
- `public_member_profiles_primary_role_check`;
- `public_member_profiles_experience_level_check`;
- `public_member_profiles_years_growing_check`;
- `public_member_profiles_connection_permission_check`;
- `public_member_profiles_username_check`;
- `public_member_profiles_invitation_preferences_check`;
- `public_member_profiles_identity_provenance_check`;
- `grow_identity_field_visibility_pkey`;
- `grow_identity_field_visibility_key_check`; and
- `grow_identity_field_visibility_value_check`.

The exact indexes within the correction and verification surface are:

- `public_member_profiles_username_unique_idx`;
- `public_member_profiles_grow_network_discovery_idx`;
- the index supporting `grow_identity_field_visibility_pkey`.

Existing identifier uniqueness and conflict behavior must be preserved. An
index or constraint may be changed only if the verified correction requires
it; new persistent constraints or indexes are not pre-authorized.

### 15.2 Read-only dependency and preservation surfaces

These exact connected tables are read-only preflight and post-execution
verification surfaces and are prohibited from mutation:

- `public.profiles`;
- `public.grow_follows`;
- `public.user_recognitions`;
- `public.recognition_definitions`;
- `public.seed_vault_share_users`; and
- `public.seed_vault_entries`.

For `public.seed_vault_share_users`, the exact preserved relationship evidence
is `id`, `owner_user_id`, `shared_with_user_id`, `can_view_quantity`,
`can_view_storage_location`, `can_view_storage_notes`, `can_view_notes`,
`created_at`, and `updated_at`, together with
`seed_vault_share_users_not_self_check`,
`seed_vault_share_users_owner_recipient_idx`,
`seed_vault_share_users_recipient_idx`, and
`seed_vault_share_users_owner_idx`. Valid shares and permission flags must not
be rewritten by Grow Identity recovery.

`pg_timezone_names` is an existing read-only system-catalog dependency of the
timezone validation path. Its connected presence and compatible behavior have
not been inspected. A separately authorized future connected preflight must
authenticate compatibility without expanding mutation authority; uncertainty
stops execution.

## 16. Connected View, Function, Trigger, Policy, and Grant Boundary

### 16.1 Permitted existing object correction surfaces

Only these existing objects may be replaced or altered by the selected
forward-only migration, and only to satisfy an already approved recovery
obligation:

- view `public.safe_public_member_profiles`;
- functions `public.get_grow_identity_contract_v1()`,
  `public.seed_grow_identity_field_visibility_v1()`,
  `public.sync_public_member_profiles_identity()`,
  `public.grow_identity_field_visible_v1(uuid,text,text)`,
  `public.get_grow_identity_v1(uuid,text)`,
  `public.search_grow_identities_v1(text,integer)`,
  `public.update_my_grow_identity_v1(jsonb)`, and
  `public.get_public_identity_and_recognition(uuid)`;
- triggers `public_member_profiles_seed_grow_identity_visibility` and
  `public_member_profiles_identity_sync` on
  `public.public_member_profiles`;
- policies `Owners can read Grow Identity field visibility` on
  `public.grow_identity_field_visibility`, `Owners can create their member
  identity` on `public.public_member_profiles`, and `Owners can update their
  member identity` on `public.public_member_profiles`; and
- grants or revocations on the two permitted tables, the permitted view, and
  the permitted functions, limited to roles `public`, `anon`,
  `authenticated`, and `service_role` and limited to the least privilege
  required by IC-GI-001.

`public.get_grow_identity_contract_v1()` may change only if the verified
correction requires removal of an evidence-free default already represented
in its response. It may not add canonical meaning. The trigger identity and
table identity must remain stable unless a separately governed update is
approved.

### 16.2 Read-only function, trigger, policy, and grant evidence

The following exact objects are read-only dependencies or verification
surfaces and are prohibited from mutation:

- `auth.uid()`;
- `public.current_user_is_admin()`;
- `public.grow_identity_field_keys_v1()`;
- `public.grow_identity_default_invitation_preferences_v1()`;
- `public.grow_identity_invitation_preferences_valid_v1(jsonb)`;
- `public.grow_identity_provenance_valid_v1(jsonb)`;
- `public.normalize_grow_identity_text_array_v1(text[],integer)`;
- `public.normalize_grow_identity_json_text_array_v1(jsonb,integer)`;
- `public.grow_identity_is_connection_v1(uuid,uuid)`;
- `public.grow_identity_has_mutual_connection_v1(uuid,uuid)`;
- `public.get_my_grow_identity()`;
- `public.can_request_grow_connection_v1(uuid)`;
- `public.get_identity_and_recognition_v1(uuid,boolean,boolean)`;
- `public.search_seed_vault_share_users(text)`;
- `public.upsert_seed_vault_user_share(uuid,boolean,boolean,boolean,boolean)`;
- `public.remove_seed_vault_user_share(uuid)`;
- `public.get_seed_vault_user_shares()`;
- `public.get_seed_vaults_shared_with_me()`;
- `public.get_direct_shared_seed_vault(uuid)`;
- `public.set_seed_vault_share_users_updated_at()`;
- trigger `seed_vault_share_users_set_updated_at`;
- policies `Seed Vault share owners can read grants`, `Seed Vault shared users
  can read their grant`, `Seed Vault owners can grant direct shares`, `Seed
  Vault owners can update direct shares`, and `Seed Vault owners can delete
  direct shares`; and
- all grants on `public.seed_vault_share_users` and its functions.

`auth.uid()` supplies the authenticated caller boundary, and
`public.current_user_is_admin()` is referenced by existing policies and
functions. Their current connected definitions, ownership, grants, and
behavior have not been inspected by this correction. A separately authorized
future connected preflight must authenticate their presence and compatibility
and must fail closed if that compatibility cannot be established.

The complete effective grant, RLS, ownership, `security definer`, search-path,
authorization, and output graph for all named objects is a mandatory read-only
preflight and verification surface. No new persistent function, view, table,
trigger, policy, constraint, index, schema, extension, role, or grant target is
authorized. Discovery that a new object is required stops execution for a
separately audited and founder-approved governance update.

## 17. Connected-Environment Mutation Boundary

There is no presently enforceable Phase 7 connected-mutation path. Before any
future Phase 7 consideration, all access to the Section 6 target is read-only
and separately authorized. The authenticated pending sequence prevents the
established mechanism from reaching the selected recovery migration within
this addendum's exact scope. A separately authorized and audited
migration-sequence resolution would be required before a future execution
instruction could be considered; this addendum grants no authority to design,
approve, or apply one.

Only if that independent gate were later satisfied could a separate Phase 7
instruction consider exact verified migration bytes or separately authorized
application deployment. Any resulting change would remain limited to the
correction surfaces in Sections 14–16. Migration execution and application
deployment remain distinct authorities and outcomes.

Every connected object outside those surfaces is prohibited from mutation.
All ten pending migrations, any ledger repair, production data repair outside
the classified rows, role creation, secret rotation, configuration change,
local relinking, and ICE-SC-003 activity remain prohibited.

## 18. Preservation and Fail-Closed Requirements

Execution must preserve the original unrelated working-tree content and every
unlisted repository byte. It must also preserve valid member values, stable
owner association, identifiers, explicit privacy choices, visibility evidence,
provenance, Recognition records, direct-Vault shares, Vault permission flags,
and source-domain ownership.

Missing visibility evidence must deny disclosure. Missing or ambiguous
Identity evidence must remain missing or ambiguous. No default, compatibility
adapter, retry, or partial state may create public access, `grower`, `new`,
verified, recognized, connected, or other canonical meaning without
attributable authority.

The existing E1 and E2 migrations are immutable read-only evidence. Any later
recovery remains forward-only, but the selected migration is not presently
eligible for creation or execution and chronology supplies no authority.

## 19. Target and Path-Boundary Verification

A future execution preflight cannot become eligible while the authenticated
ten-migration sequence lacks a separately authorized and audited resolution.
The repository-pinned Supabase CLI `2.109.1` and the established migration
mechanism expose no authenticated version cutoff or exact-file selector that
can apply only the authorized Grow Identity scope. This addendum defines no
bounded execution command and authorizes no migration-selection workaround.

If an independently governed resolution were later established, a fresh
execution preflight would still have to complete the following checks before
any repository or connected write:

1. reverify approved hashes and governance state for IC-GI-001,
   ICE-GI-001-1, and this addendum;
2. verify the exact branch, HEAD, staged state, complete working-tree
   attribution, and absence of any unapproved path;
3. verify every modification-authorized path against Section 9 and every
   connected object against Sections 14–16;
4. reverify that the Vercel production binding and Supabase project metadata
   agree on exactly the Section 6 target;
5. under separate connected-read authority, inspect the exact target's present
   ledger, schema, roles, policies, grants, functions, data categories,
   backup/recovery boundary, and partial state read-only, including the
   presence and compatibility of `auth.uid()`,
   `public.current_user_is_admin()`, and `pg_timezone_names`;
6. reauthenticate the terminal remote version, the exact ten-version pending
   tail, and the absence or presence of every Grow Identity version;
7. prove before execution that an exact command can apply no more than the
   exact separately authorized scope; absence of that proof is a stop;
8. verify that the selected recovery-migration path remains absent and ordered
   after every existing migration without treating chronology as authority;
9. establish an isolated disposable local database for all write rehearsal;
   and
10. return an effective conditional execution-authorization decision before
    any execution phase writes.

No command that could apply more than the exact authorized scope is permitted.
Later ledger inspection cannot make an unbounded command retrospectively
acceptable. File movement or exclusion, renaming, timestamp or version
changes, migration editing, ledger marking, baselining, history repair,
sequence reconstruction, and ad hoc SQL are prohibited as execution-selection
workarounds. A separately authorized and audited resolution is required before
any execution instruction could be considered, and this addendum grants no
authority to design, approve, or apply that resolution.

Before Phase 7, verify exact migration bytes, exact deployed client bytes,
operator identity, cutover window, backup/recovery readiness, stop boundary,
and evidence location under separately granted target-specific authority.

## 20. Unexpected State and Partial Application

Unexpected repository state, target drift, ledger drift, partial prior
application, an uncertain write, or a mismatch between deployed and reviewed
bytes stops work. No blind retry is permitted.

No state below is asserted to have occurred. If a separately authorized future
execution encounters the Grow Identity migrations, it must distinguish:

1. neither Grow Identity migration started;
2. the first migration started but completion is unconfirmed;
3. the first migration failed or rolled back;
4. the first migration completed and the second did not start;
5. the first migration completed and the second failed;
6. the first migration completed and the second was interrupted;
7. second-migration completion is uncertain;
8. connection loss or timeout made the resulting ledger state uncertain;
9. both migrations completed but post-execution verification has not occurred;
   and
10. both migrations completed and later verification found a nonconformance.

Every such state requires exact ledger evidence and exact command and output
evidence when available. Uncertainty remains uncertainty when completion
cannot be authenticated. Work stops before another write. No blind retry,
presumption of rollback, cross-migration atomicity, idempotency, history
repair, or ad hoc corrective SQL is permitted. Any further mutation requires a
fresh, separately authorized recovery decision.

An interruption record may use only the activated Phase 8 paths and must state
completed and incomplete phases, exact repository and target state, active
write authority, preserved evidence, failed gate, and safe restart condition.
It must not claim completion.

## 21. Stop Conditions

Execution stops before the next action if:

- any required authority, approval, closure, or preflight result is absent;
- target identity does not resolve exactly to Section 6;
- production and Supabase evidence disagree or a preview cannot be excluded;
- any repository modification would touch an unlisted or inactive conditional
  path;
- any connected mutation would touch an unlisted object;
- implementation requires a new database object or a second migration;
- corrective SQL would need new canonical meaning or another capability;
- schema, data, ownership, privacy, provenance, Recognition, or Vault-purpose
  state cannot be classified safely;
- migration order, ledger state, backup, rollback, completion, or retry safety
  is uncertain;
- the authenticated ten-migration pending sequence remains unresolved by a
  separately authorized and audited governance decision;
- the contemplated command lacks an authenticated exact-version cutoff or
  exact-file selector, or could apply more than the authorized scope;
- any execution-selection approach would require file movement or exclusion,
  renaming, timestamp or version changes, migration editing, ledger marking,
  baselining, history repair, sequence reconstruction, or ad hoc SQL;
- any action would generate, compare, recalculate, replace, supersede,
  reapprove, or otherwise alter the approved security fingerprint or counts;
- any action would authorize P1, alter P1's withheld present-day acceptance,
  or change the IC-GC-002C six-resource security-correction branch;
- a required test is absent, failed, flaky, weakened, or unattributable;
- unrelated repository content cannot be preserved exactly;
- deployed bytes or configuration differ from reviewed bytes; or
- any action would execute or restart ICE-SC-003.

An unlisted path or object may not be added during execution. It requires a
separately authored, audited, founder-approved, repository-closed governance
update followed by a fresh strict read-only preflight.

## 22. Authorization Validity Conditions

This approved and repository-adopted addendum can participate in execution
authorization only after:

1. a repeat strict read-only Architecture and Execution-Governance Audit
   returns PASS on the exact corrected substantive bytes;
2. the founder explicitly approves those exact audited bytes;
3. approval-state normalization and exact execution-index registration are
   separately authorized and Git-closed without substantive change;
4. a separately authorized and audited migration-sequence resolution, if later
   directed, establishes a safe path without relying on this addendum for its
   design or authority;
5. a fresh strict read-only execution preflight passes at the resulting exact
   repository state;
6. the Section 6 target and Section 9 path boundary remain exact;
7. an effective conditional execution-authorization decision identifies the
   allowed phases and conditions; and
8. a separate execution instruction is issued.

Conditions 1 through 3 are completed by the recorded PASS audit, explicit
founder approval, and this repository adoption. Conditions 4 through 8 remain
unsatisfied and separately governed.

Phase 6 additionally requires exact-target read-only authority. Phase 7
additionally requires target-specific migration, deployment, production,
operator, rollback, and evidence authority. Phase 8 requires separate
governance-closure and, where applicable, production-ledger authority.

Founder approval and repository adoption do not authorize connected execution.
Execution success does not constitute post-execution verification. Execution
success, verification, or governance closure does not constitute founder or
present-day production acceptance. Verification does not authorize application
deployment. No migration-history authority exists under this addendum.

## 23. Automatic Expiration

Any execution authorization relying on this addendum automatically becomes
ineffective before the next write if:

- this addendum, IC-GI-001, or ICE-GI-001-1 substantive bytes or governance
  state change;
- the approved repository commit, branch, working-tree boundary, selected
  migration filename, or allowlist changes;
- the production deployment binding, Supabase project reference, hostname,
  organization, environment role, or project identity changes;
- an unlisted path or connected object is required;
- another migration affecting a named object is introduced before execution;
- the authenticated ten-version pending sequence or its governance state
  changes, remains unresolved, or is addressed without a separately authorized
  and audited migration-sequence decision;
- the approved security fingerprint
  `40b325fb4240387ed6eed8f74366f5fd61d3cf886c5f1b1f7f14ac203d024f56`,
  approved counts `48 / 1 / 0 / 105 / 142 / 5`, P1 disposition, or the
  IC-GC-002C six-resource security-correction branch changes;
- the preflight evidence ages past a separately stated authorization window or
  any material target state changes;
- a migration attempt begins, fails, is interrupted, or has uncertain
  completion beyond the authorized retry model; or
- a stop condition in IC-GI-001, ICE-GI-001-1, or this addendum occurs.

Expiration authorizes no cleanup, retry, rollback, relink, repair, or closure.
A fresh governing decision and preflight are required.

## 24. Execution and Production Non-Authorization

ICE-GI-001-1 remains unexecuted. This approved and repository-adopted addendum
does not authorize
implementation, test modification, corrective migration creation, SQL design,
migration execution, deployment, production access, production mutation,
connected-environment mutation, recovery, closure, migration-sequence design,
migration-history action, security-fingerprint work, founder acceptance, or
present-day production acceptance. There is no currently authenticated
bounded-execution command or connected-mutation path.

The execution-governance sequence remains, in order:

1. repeat Architecture and Execution-Governance Audit of the exact corrected
   addendum;
2. explicit founder approval;
3. repository closure of the approved addendum;
4. a separately authorized and audited migration-sequence resolution, if the
   founder later directs that separate governance work;
5. a fresh strict read-only execution preflight;
6. an effective conditional execution-authorization decision; and
7. a separately instructed execution run.

Steps 1 through 3 are complete. Steps 4 through 7 remain unauthorized and
incomplete.

No authority is granted for ICE-SC-003.

## 25. Success and Closure Criteria

This addendum resolves its authorship purpose only when an audit confirms that
one target is bound and the repository and connected-object boundaries are
exact, complete, minimal, fail-closed, and non-expansive. That governance
success is distinct from ICE execution success.

Technical execution success is not post-execution verification. Verification
success is not founder acceptance, present-day production acceptance, or
application deployment authority. Governance closure truthfully records a
complete or interrupted governed result but does not itself constitute founder
or present-day production acceptance. Each later state requires its own exact
authority and evidence.

ICE-GI-001-1 execution may be closed complete only after every applicable
invariant and gate in ICE-GI-001-1 passes locally and, under separate authority,
against the exact target; the exact allowlisted diff is verified; the selected
migration and deployed bytes are attributable; and the exact closure paths
truthfully record the result. An interrupted result must remain interrupted.
No execution can be closed complete while migration completion or the ledger
is uncertain, while post-execution verification is absent or failed, or while
the authenticated command could have exceeded its exact authorized scope.

Neither closure outcome restarts ICE-SC-003 automatically.

## 26. Definitions

- **Exact path** — one complete repository-relative filename with no pattern,
  directory-level authority, alias, or unnamed future substitute.
- **Selected future path** — an exact absent filename reserved as a boundary;
  selection alone does not authorize creation.
- **Mandatory path** — a path that must change if execution reaches the stated
  phase and condition; it is not permission to change early.
- **Conditional path** — a path prohibited until objective evidence satisfies
  its one stated activation condition.
- **Read-only evidence** — information that may be inspected but not modified,
  executed as a write, or promoted to canonical authority.
- **Repository adoption** — separately authorized recording and Git closure of
  exact approved governance bytes; it is not implementation or execution.
- **Execution success** — authenticated technical completion of an authorized
  operation; it is not verification, acceptance, or deployment.
- **Post-execution verification** — separately authorized evaluation of exact
  post-write evidence against governing requirements; success does not imply
  founder acceptance or deployment.
- **Founder acceptance** — an explicit founder disposition of exact verified
  evidence; it is not supplied by execution success or governance closure.
- **Present-day production acceptance** — explicit recognition of exact
  implementation as the accepted current production baseline.
- **Migration-history authority** — separately granted authority to alter,
  mark, repair, baseline, or reconstruct migration history; none exists here.
- **Migration-sequence resolution** — separately governed authority and design
  addressing an authenticated sequence blocker; this addendum neither supplies
  nor prescribes one.
- **Governed connected target** — the single Section 6 Supabase project bound
  to the production Grow deployment.
- **Correction surface** — an exact existing object that later verified SQL
  may alter only for an already approved ICE-GI-001-1 obligation.
- **Verification surface** — an exact object or path that must be inspected or
  tested but is not thereby mutation-authorized.
- **Closure-only** — prohibited until a complete or interrupted execution gate
  and separate closure authority are both effective.
- **Substantive bytes** — content establishing authority, target, boundary,
  obligation, gate, classification, stop condition, or closure criterion.
- **Metadata-only normalization** — recording an already completed audit or
  founder decision without changing substantive bytes.

## 27. Decision History

| Date | Decision | Effect |
| --- | --- | --- |
| 2026-07-31 | Proposed addendum authored from read-only governing, repository, Vercel production-binding, and Supabase project-metadata evidence. | Records one proposed target binding and one proposed exact execution boundary. No audit, founder approval, execution authorization, implementation, migration, deployment, production action, connected mutation, or ICE-SC-003 activity occurred. |
| 2026-07-31 | Strict Architecture and Execution-Governance Audit returned `BLOCKED — ICE-GI-001-1A REQUIRES REVISION`. | Identified six bounded defects. The audit granted no founder approval, repository adoption, implementation, sequence-resolution, connected-execution, migration-history, fingerprint, deployment, verification, or acceptance authority. |
| 2026-07-31 | Founder-authorized bounded correction pass applied only the six audited correction units. | Leaves the corrected artifact proposed and unaudited. Repeat Architecture and Execution-Governance Audit is required; founder approval, repository adoption, connected execution, migration-history action, fingerprint work, and database action remain unauthorized. |
| 2026-07-31 | Repeat strict read-only Architecture and Execution-Governance Audit returned `PASS — ICE-GI-001-1A ARCHITECTURE AND EXECUTION-GOVERNANCE AUDIT` on the exact corrected artifact at SHA-256 `19a6e99fe4b5d25368f74bf3221c6a7f346c49cbf7fb4199ee6eaa05505c705c`. | Establishes that the exact corrected substantive content is architecturally and execution-governance sound for founder review. The audit grants no founder approval, repository adoption, implementation, connected execution, migration-history, verification, deployment, or acceptance authority. |
| 2026-07-31 | Founder explicitly approved the exact PASS-audited ICE-GI-001-1A substantive content represented by SHA-256 `19a6e99fe4b5d25368f74bf3221c6a7f346c49cbf7fb4199ee6eaa05505c705c`. | Grants founder approval only to the exact audited substantive authority and boundaries. It does not authorize repository adoption, implementation, migration-sequence resolution, connected execution, migration-history action, verification, deployment, fingerprint work, P1 reconsideration, IC-GC-002C correction execution, or acceptance. |
| 2026-07-31 | Separately authorized repository adoption normalized only audit, founder-approval, and repository-adoption state and registered the approved addendum in the execution index. | Repository adoption is complete without substantive change. Connected execution, migration-sequence resolution, implementation, migration application, verification, deployment, fingerprint work, P1 reconsideration, IC-GC-002C correction execution, and database action remain unauthorized. |
